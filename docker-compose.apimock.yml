version: "3"

services:

  frontend:
    depends_on:
      - apimock
      - nginx

  apimock:
    build:
      context: ./apimock
    image: apimock-just_finance
    container_name: apimock-just_finance
    hostname: apimock-just_finance
    command: npm run apimock:watch
    network_mode: "host"
    expose:
      - "8000"
    env_file:
      - .env
    volumes:
      - ./apimock/server:/apimock/server
